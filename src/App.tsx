import { center } from "../styled-system/patterns";

function App() {
  return (
    <div
      className={center({
        h: "100vh",
      })}
    >
      <p>"npm run storybook"</p>
    </div>
  );
}

export default App;
